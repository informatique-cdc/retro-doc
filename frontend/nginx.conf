server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml;

    client_max_body_size 50m;

    # API proxy pass
    location /api/ {
        proxy_pass ${API_URL}/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # The chat endpoints stream Server-Sent Events. Buffering, caching or
        # compressing the response would hold tokens back until a buffer fills,
        # turning a live answer into a few large bursts. Clearing `Connection`
        # keeps the upstream connection on HTTP/1.1 keep-alive so the chunked
        # body is forwarded as it arrives.
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;
        gzip off;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        # An agent run holds the stream open across tool calls, well past the
        # 60s a request/response endpoint needs.
        proxy_read_timeout 600s;
    }

    # Disable cache for index.html to ensure latest app version is served
    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }

    # Angular app - serve static files and handle client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
